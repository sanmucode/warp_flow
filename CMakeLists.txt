cmake_minimum_required(VERSION 2.8)
project( warpFlow )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( warpFlow_gpu warp_flow_gpu.cpp )
target_link_libraries( warpFlow_gpu ${OpenCV_LIBS} )